import {ColonyPlanner} from "./ColonyPlanner";
import {Logger} from "@utils/Logger";

export class Planner {
  protected logger = new Logger("Planner");
  public plan(colonyPlanner: ColonyPlanner): void {
    // to implement
  }
}
